{
  "name": "przydas-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/normalize-subscription-signup.mjs && astro dev",
    "build": "node scripts/normalize-subscription-signup.mjs && node scripts/audit-subscription-readiness.mjs && astro build",
    "preview": "astro preview",
    "audit:subscription": "node scripts/normalize-subscription-signup.mjs && node scripts/audit-subscription-readiness.mjs"
  },
  "dependencies": {
    "astro": "7.3.2"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
